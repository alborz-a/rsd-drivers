const React = require('react');
const { View } = require('react-native-web');

const BlurView = (props) => React.createElement(View, props);
const VibrancyView = (props) => React.createElement(View, props);

module.exports = {
  BlurView,
  VibrancyView,
};
